export type {
    Video,
    VideoRevision,
    UploadSession,
    User,
    UserVideoReadStatus,
    Identity,
    VideoComment,
} from "@prisma/client";

export { UploadStorageType } from "@prisma/client";
export { Prisma as PrismaTypes } from "@prisma/client";